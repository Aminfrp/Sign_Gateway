import { ReactNode } from "react";

export type CardType = {
  children: ReactNode;
  className?: string;
};

export type CardHeaderType = {
  children: ReactNode;
  className?: string;
};

export type CardBodyType = {
  children: ReactNode;
  className?: string;
};
